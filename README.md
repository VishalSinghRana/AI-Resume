AI Resume.

Created application using:- npx create-next-app@latest ai-resume

When prompted: TypeScript → Yes Tailwind → Yes App Router → Yes ESLint → Yes

To run:- npm run dev

IF ERROR OCCURS:- Run: npm install

Install framer library. npm install framer-motion